apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "KMS-generic-helm-charts.fullname" . }}-test-connection"
  labels:
    {{- include "KMS-generic-helm-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "KMS-generic-helm-charts.fullname" . }}:{{ .Values.kms-generic.service.port }}']
  restartPolicy: Never
